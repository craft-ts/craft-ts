// NE PAS MODIFIER : ce fichier est l'overlay utilisé par `nx serve demo`.
// Pour ajouter/modifier une route, éditer uniquement `app.routes.ts`.
// En dehors du serveur de démo, ce relais conserve toutes les routes actives.
export { demoEnabledRoutePaths, demoRoutes } from './app.routes';
