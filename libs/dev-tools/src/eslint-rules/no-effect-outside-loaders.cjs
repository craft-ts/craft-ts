// Kept as the descriptive public name. The original file remains as a
// compatibility entry point for projects that already enabled the old rule.
module.exports = require('./no-effect-in-params.cjs');
